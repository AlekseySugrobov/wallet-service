openapi: 3.0.1
info:
  title: wallet-service
  version: '1.0'


servers:
  - url: 'http://localhost:3000'

paths:
  /api/v1/player/{id}:
    get:
      summary: Returns player info
      tags: [ "Player" ]
      parameters:
        - in: path
          name: id
          schema:
            type: integer
          required: true
          description: Player id
      responses:
        '200':
          description: OK
